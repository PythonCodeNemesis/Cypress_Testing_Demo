# Cypress_Testing_Demo

Run the backend with:

```
python3 app.py
```

Run the frontend with:

```
cd my-app
npm start
```

Open Cypress with:

```
npx cypress open
```

Run Cypress with:

```
npx cypress run
```
